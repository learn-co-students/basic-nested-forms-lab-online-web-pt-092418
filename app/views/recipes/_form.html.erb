<%= form_for recipe do |f| %>
  Title: <%= f.text_field :title %>
  <br>
  <%= f.fields_for :ingredients do |ff| %>
    Ingredient Name <%= ff.text_field :name %>
    Ingredient Quantity <%= ff.number_field :quantity %>
  <% end %>
  <%= f.submit %>
<% end %>
